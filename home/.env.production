# Defaults for production builds. Override in the host's environment settings.
# The public relay: the home and the apps it serves must share one to sync.
VITE_SIGNALING_URL=wss://p2p-web-relay.fly.dev
